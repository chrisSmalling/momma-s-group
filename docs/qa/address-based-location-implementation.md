# Address-based personalized location implementation

Members enter an address in Settings. The server geocodes it with the existing OpenRouteService integration; the returned coordinates are internal routing cache values and are never manually entered by the member.
