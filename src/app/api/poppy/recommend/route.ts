import { randomUUID } from "node:crypto";
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { getRoutingProvider } from "@/lib/routing";
import { applyDriveTimeGate } from "@/lib/routing/driveTimeGate";
import { getWeatherContext, type WeatherContext } from "@/lib/weather-context";
import type { PlaceHours } from "@/types";
import { isFreeCost } from "@/lib/cost";
import { parseIntent } from "@/lib/recommend/intent";
import { detectPlaceSearchTerm } from "@/lib/recommend/placeIntent";
import { searchPlaces } from "@/lib/places/search";
import { toPlaceSearchCandidate } from "@/lib/recommend/placeSearchCandidate";
import { buildPlaceSearchResponseText } from "@/lib/recommend/placeSearchText";
import { buildCacheKey } from "@/lib/recommend/cacheKey";
import { buildFallbacks, buildResponseText, recommend } from "@/lib/recommend/recommend";
import { generatePoppyLine } from "@/lib/recommend/poppyText.server";
import type { IndoorPreference, Mood, PoppyCandidate, PoppyProfile, RecommendationConstraints, RecommendationResult } from "@/lib/recommend/types";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
const MESSAGE_MAX = 400; const CANDIDATE_POOL = 500; const CACHE_TTL_MINUTES = 15; const MAX_DRIVE_MINUTES = 45;
const MOODS: Mood[] = ["all", "indoor", "outdoor", "water", "active", "learn", "create", "animals"]; const INDOOR: IndoorPreference[] = ["indoor", "outdoor", "either"];
function sanitizePrevious(input: unknown): Partial<RecommendationConstraints> | undefined { if (!input || typeof input !== "object") return undefined; const raw=input as Record<string,unknown>; const out:Partial<RecommendationConstraints>={}; if(typeof raw.mood==="string"&&MOODS.includes(raw.mood as Mood))out.mood=raw.mood as Mood;if(typeof raw.indoor==="string"&&INDOOR.includes(raw.indoor as IndoorPreference))out.indoor=raw.indoor as IndoorPreference;if(raw.budget==="free"||raw.budget==="budget"||raw.budget==="any")out.budget=raw.budget;if(raw.timeframe==="today"||raw.timeframe==="tomorrow"||raw.timeframe==="weekend"||raw.timeframe==="any")out.timeframe=raw.timeframe;if(typeof raw.maxMiles==="number"&&Number.isFinite(raw.maxMiles)&&raw.maxMiles>0&&raw.maxMiles<=200)out.maxMiles=raw.maxMiles;if(raw.maxMiles===null)out.maxMiles=null;if(typeof raw.indoorExplicit==="boolean")out.indoorExplicit=raw.indoorExplicit;return out; }
function toProfile(row:Record<string,unknown>|null):PoppyProfile{const arr=(v:unknown):string[]=>Array.isArray(v)?v.filter((x):x is string=>typeof x==="string"):[];const num=(v:unknown):number|null=>typeof v==="number"&&Number.isFinite(v)?v:null;const str=(v:unknown):string|null=>typeof v==="string"&&v.trim()?v:null;const indoorPref=row?.indoor_preference;return{childAgeMonths:num(row?.child_age_months),childInterests:arr(row?.child_interests),childActivityPreferences:arr(row?.child_activity_preferences),preferredCategories:arr(row?.preferred_categories),preferredPlaceTypes:arr(row?.preferred_place_types),indoorPreference:indoorPref==="indoor"||indoorPref==="outdoor"?indoorPref:"either",maxDistanceMiles:num(row?.max_distance_miles),familyBudgetNote:str(row?.family_budget_note),napStart:str(row?.nap_start),napEnd:str(row?.nap_end),homeLat:num(row?.home_lat),homeLng:num(row?.home_lng)};}
type PoppyCandidateRow={id:string;title:string|null;display_title:string|null;description:string|null;venue_name:string|null;venue_display:string|null;address:string|null;location_city:string|null;location_state:string|null;location_zip:string|null;lat:number|null;lng:number|null;location_latitude:number|null;location_longitude:number|null;starts_at:string|null;ends_at:string|null;age_min_months:number|null;age_max_months:number|null;age_band:string|null;age_tags:string[]|null;cost:string|null;source:string|null;source_url:string|null;registration_required:boolean|null;registration_url:string|null;is_outdoor:boolean;experience_type:string|null;weather_fit:string|null;verification_tier:string|null;content_review_status:string|null;last_verified_at:string|null;place_id:string|null;program_id:string|null;kind:"event"|"place";hours:PlaceHours|null;season_start:string|null;season_end:string|null};
type PlaceTrustRow={id:string;stroller_accessible:boolean|null;has_changing_table:boolean|null;nursing_friendly:boolean|null;parking_notes:string|null;typical_crowd_note:string|null;best_time_note:string|null;what_to_bring:string[]|null};type TipRow={place_id:string|null;event_id:string|null;body:string};
function candidateToPoppyCandidate(row:PoppyCandidateRow,place?:PlaceTrustRow,tips:string[]=[]):PoppyCandidate{const isPlace=row.kind==="place";return{kind:row.kind,id:row.id,title:row.display_title?.trim()||row.title?.trim()||"Family activity",description:row.description,venue:row.venue_display?.trim()||row.venue_name?.trim()||null,room_name:null,organizer:null,address:row.address,lat:row.lat??row.location_latitude,lng:row.lng??row.location_longitude,location_latitude:row.location_latitude??row.lat,location_longitude:row.location_longitude??row.lng,starts_at:row.starts_at,ends_at:row.ends_at,time_precision:isPlace?"unknown":"datetime",time_unknown:isPlace,time_unknown_reason:isPlace?"This is an always-open place, not a scheduled event.":null,cost:row.cost,is_free:isFreeCost(row.cost),age_tags:row.age_tags??[],age_min_months:row.age_min_months,age_max_months:row.age_max_months,age_band:row.age_band,is_outdoor:row.is_outdoor,what_to_bring:place?.what_to_bring??[],registration_required:row.registration_required,registration_url:row.registration_url,source:row.source??"",source_id:null,source_url:row.source_url,content_status:row.content_review_status,geography_tier:row.verification_tier,experience_type:row.experience_type,weather_fit:row.weather_fit,place_id:row.place_id,program_id:row.program_id,proposed_by_group:null,metro_area:row.location_city??"",status:"published",last_verified_at:row.last_verified_at,added_by:null,hours:row.hours,season_start:row.season_start,season_end:row.season_end,trust_stroller_accessible:place?.stroller_accessible??null,trust_changing_table:place?.has_changing_table??null,trust_nursing_friendly:place?.nursing_friendly??null,trust_parking_notes:place?.parking_notes??null,trust_typical_crowd_note:place?.typical_crowd_note??null,trust_best_time_note:place?.best_time_note??null,trust_community_tips:tips} as PoppyCandidate;}
async function applyDriveTimeHardGate(events:PoppyCandidate[],origin:{lat:number;lng:number}|null){const gate=await applyDriveTimeGate(events,origin,MAX_DRIVE_MINUTES);return{events:gate.kept,available:gate.available};}
export async function POST(request:Request){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Please sign in."},{status:401});let body:Record<string,unknown>;try{body=await request.json() as Record<string,unknown>}catch{return NextResponse.json({error:"Invalid request."},{status:400})}const message=typeof body.message==="string"?body.message.slice(0,MESSAGE_MAX):"";const previous=sanitizePrevious(body.previous);const originMode=body.originMode==="current"?"current":"home";try{const{data:profileRow}=await supabase.from("profiles").select("display_name, child_name, child_age_months, child_interests, child_activity_preferences, family_budget_note, preferred_categories, preferred_place_types, indoor_preference, max_distance_miles, nap_start, nap_end, home_address, home_lat, home_lng").eq("id",user.id).maybeSingle();const profile=toProfile(profileRow as Record<string,unknown>|null);const childName=typeof profileRow?.child_name==="string"&&profileRow.child_name.trim()?profileRow.child_name.trim():null;const hasSavedHomeAddress=typeof profileRow?.home_address==="string"&&profileRow.home_address.trim().length>0;let origin:{lat:number;lng:number}|null=profile.homeLat!=null&&profile.homeLng!=null?{lat:profile.homeLat,lng:profile.homeLng}:null;if(originMode==="current"){const o=body.origin as Record<string,unknown>|undefined;if(o&&typeof o.lat==="number"&&typeof o.lng==="number"&&Number.isFinite(o.lat)&&Number.isFinite(o.lng))origin={lat:o.lat,lng:o.lng};}const constraints=parseIntent(message,previous);if(constraints.maxMiles==null&&profile.maxDistanceMiles!=null)constraints.maxMiles=profile.maxDistanceMiles;const now=new Date();const fallbackRequestId=randomUUID();
// "Where can I take her for gymnastics?" is a place search, not an event
// query — category_tags is too coarse a vocabulary for a specific activity
// (see search_places' migration comment), so this routes to the dedicated
// places backbone instead of the mood/event pipeline below.
const placeSearchTerm=detectPlaceSearchTerm(message);if(placeSearchTerm)return handlePlaceSearch({supabase,userId:user.id,message,placeSearchTerm,origin,profile,constraints,childName,hasSavedHomeAddress,fallbackRequestId});
const cacheKey=buildCacheKey(user.id,constraints,profile,origin,now);const{data:cached,error:cacheReadError}=await supabase.from("recommendation_response_cache").select("response, expires_at").eq("request_hash",cacheKey).eq("user_id",user.id).gt("expires_at",now.toISOString()).maybeSingle();if(cacheReadError)console.error("[poppy] cache read failed",cacheReadError.message);if(cached?.response){const payload=cached.response as RecommendationResult;const requestId=await recordRecommendationRequest(supabase,user.id,message,constraints,payload.candidates,"poppy-cache");if(requestId)await recordRecommendationAudit(supabase,requestId,constraints,payload.candidates);return NextResponse.json({...payload,requestId:requestId??fallbackRequestId,cacheHit:true,locationStatus:origin?"ready":hasSavedHomeAddress?"saved_address_needs_geocoding":"missing"});}
// Phase A: time eligibility is per-kind, not a single WHERE clause — an
// event must not have started yet; a place has no occurrence to compare
// against `now` at all (its season/hours define eligibility instead, see
// filter.ts). Places are ordered first so they can never be starved out of
// the pool by a large batch of upcoming events.
const{data:candidateRows,error:candidateError}=await supabase.from("poppy_recommendation_candidates").select("id, title, display_title, description, venue_name, venue_display, address, location_city, location_state, location_zip, lat, lng, location_latitude, location_longitude, starts_at, ends_at, age_min_months, age_max_months, age_band, age_tags, cost, source, source_url, registration_required, registration_url, is_outdoor, experience_type, weather_fit, verification_tier, content_review_status, last_verified_at, place_id, program_id, kind, hours, season_start, season_end").or(`and(kind.eq.event,starts_at.gte.${now.toISOString()}),kind.eq.place`).order("kind",{ascending:false}).order("starts_at",{ascending:true}).limit(CANDIDATE_POOL);if(candidateError)throw candidateError;const rows=(candidateRows??[]) as PoppyCandidateRow[];const placeIds=[...new Set(rows.map(r=>r.place_id).filter((id):id is string=>Boolean(id)))];const eventIds=rows.map(r=>r.id);const placeMap=new Map<string,PlaceTrustRow>();const tipsByTarget=new Map<string,string[]>();if(placeIds.length>0){const{data:placeRows,error}=await supabase.from("places").select("id, stroller_accessible, has_changing_table, nursing_friendly, parking_notes, typical_crowd_note, best_time_note, what_to_bring").in("id",placeIds);if(error)console.error("[poppy] place trust enrichment failed",error.message);for(const place of(placeRows??[]) as PlaceTrustRow[])placeMap.set(place.id,place);const{data:tips,error:tipsError}=await supabase.from("place_tips").select("place_id, event_id, body").in("place_id",placeIds).limit(300);if(tipsError)console.error("[poppy] community tip enrichment failed",tipsError.message);for(const tip of(tips??[]) as TipRow[]){if(!tip.body?.trim())continue;const key=tip.place_id??"";tipsByTarget.set(key,[...(tipsByTarget.get(key)??[]),tip.body.trim()].slice(0,3));}}if(eventIds.length>0){const{data:eventTips,error:eventTipsError}=await supabase.from("place_tips").select("place_id, event_id, body").in("event_id",eventIds).limit(300);if(eventTipsError)console.error("[poppy] event community tip enrichment failed",eventTipsError.message);for(const tip of(eventTips??[]) as TipRow[]){if(!tip.body?.trim())continue;const key=tip.event_id??"";tipsByTarget.set(key,[...(tipsByTarget.get(key)??[]),tip.body.trim()].slice(0,3));}}let eventList=rows.map(row=>candidateToPoppyCandidate(row,row.place_id?placeMap.get(row.place_id):undefined,tipsByTarget.get(row.id)??(row.place_id?tipsByTarget.get(row.place_id)??[]:[])));const hardGate=await applyDriveTimeHardGate(eventList,origin);if(origin&&!hardGate.available){return NextResponse.json({requestId:fallbackRequestId,intent:constraints,candidates:[],responseText:"I can’t verify drive times right now, so I’m not going to guess which options are within 45 minutes.",fallbacks:buildFallbacks(constraints),cacheHit:false,locationStatus:"routing_unavailable"});}eventList=hardGate.events;let weather:WeatherContext|null=null;if(origin){try{weather=await getWeatherContext(origin.lat,origin.lng)}catch{weather=null}}const{candidates,droppedCount}=recommend({events:eventList},constraints,profile,origin,weather,now,50);await enrichDriveTimes(candidates,origin,eventList);candidates.sort((a,b)=>b.score-a.score);candidates.splice(5);let responseText=buildResponseText(candidates,constraints,childName);let model="poppy-deterministic";if(candidates.length>0){const enhanced=await generatePoppyLine({candidates,constraints,childName,message});if(enhanced){responseText=enhanced;model="poppy-gemini";}}if(!origin&&hasSavedHomeAddress)responseText=`${responseText} Your home address is saved, but its map location still needs to be verified, so distance sorting is temporarily limited.`;const requestId=await recordRecommendationRequest(supabase,user.id,message,constraints,candidates,model);const effectiveRequestId=requestId??fallbackRequestId;const result:RecommendationResult={requestId:effectiveRequestId,intent:constraints,candidates,responseText,fallbacks:candidates.length===0?buildFallbacks(constraints):[],cacheHit:false};if(requestId){await recordRecommendationAudit(supabase,requestId,constraints,candidates);const expiresAt=new Date(now.getTime()+CACHE_TTL_MINUTES*60*1000).toISOString();const{error:cacheWriteError}=await supabase.from("recommendation_response_cache").upsert({request_hash:cacheKey,user_id:user.id,request_id:requestId,response:result,model,created_at:now.toISOString(),expires_at:expiresAt},{onConflict:"request_hash"});if(cacheWriteError)console.error("[poppy] cache write failed",cacheWriteError.message);}void droppedCount;return NextResponse.json({...result,locationStatus:origin?"ready":hasSavedHomeAddress?"saved_address_needs_geocoding":"missing"});}catch(err){console.error("[poppy] recommendation failed",err);return NextResponse.json({error:"Something went wrong while Poppy was looking."},{status:500});}}
type SupabaseClient=Awaited<ReturnType<typeof createClient>>;type RecommendationCandidate=RecommendationResult["candidates"][number];
const PLACE_SEARCH_MAX_RESULTS=5;
async function handlePlaceSearch(opts:{supabase:SupabaseClient;userId:string;message:string;placeSearchTerm:string;origin:{lat:number;lng:number}|null;profile:PoppyProfile;constraints:RecommendationConstraints;childName:string|null;hasSavedHomeAddress:boolean;fallbackRequestId:string;}){
  const{supabase,userId,message,placeSearchTerm,origin,profile,constraints,childName,hasSavedHomeAddress,fallbackRequestId}=opts;
  try{
    const outcome=await searchPlaces(supabase,{term:placeSearchTerm,origin,childAgeMonths:profile.childAgeMonths,limit:30});
    if(origin&&!outcome.routingAvailable){
      return NextResponse.json({requestId:fallbackRequestId,intent:constraints,candidates:[],responseText:"I can’t verify drive times right now, so I’m not going to guess which options are within 45 minutes.",fallbacks:buildFallbacks(constraints),cacheHit:false,locationStatus:"routing_unavailable"});
    }
    const candidates=outcome.results.slice(0,PLACE_SEARCH_MAX_RESULTS).map((r,i)=>toPlaceSearchCandidate(r,profile.childAgeMonths,i));
    const responseText=buildPlaceSearchResponseText(placeSearchTerm,candidates,childName);
    const requestId=await recordRecommendationRequest(supabase,userId,message,constraints,candidates,"poppy-place-search");
    const effectiveRequestId=requestId??fallbackRequestId;
    if(requestId)await recordRecommendationAudit(supabase,requestId,constraints,candidates);
    const result:RecommendationResult={requestId:effectiveRequestId,intent:constraints,candidates,responseText,fallbacks:candidates.length===0?buildFallbacks(constraints):[],cacheHit:false};
    return NextResponse.json({...result,locationStatus:origin?"ready":hasSavedHomeAddress?"saved_address_needs_geocoding":"missing"});
  }catch(err){
    console.error("[poppy] place search failed",err);
    return NextResponse.json({error:"Something went wrong while Poppy was looking."},{status:500});
  }
}async function recordRecommendationRequest(supabase:SupabaseClient,userId:string,message:string,constraints:RecommendationConstraints,candidates:RecommendationResult["candidates"],model:string):Promise<string|null>{const{data,error}=await supabase.rpc("record_recommendation_execution",{p_user_id:userId,p_raw_prompt:message,p_intent:"discover",p_constraints:constraints,p_candidate_count:candidates.length,p_selected_ids:candidates.map(candidate=>candidate.id),p_model:model});if(error){console.error("[poppy] recommendation request audit failed",error.message);return null;}return typeof data==="string"?data:null;}async function recordRecommendationAudit(supabase:SupabaseClient,requestId:string,constraints:RecommendationConstraints,candidates:RecommendationResult["candidates"]){if(candidates.length===0)return;const rows=candidates.map((candidate,index)=>({request_id:requestId,candidate_kind:candidate.type,candidate_id:candidate.id,hard_filters:constraints,score:candidate.score,explanation:{rank:index+1},passed_hard_filters:true,filter_reasons:[],source_kind:candidate.type,source_snapshot:{}}));const{error}=await supabase.from("recommendation_audit").insert(rows);if(error)console.error("[poppy] recommendation audit failed",error.message);}async function enrichDriveTimes(candidates:RecommendationResult["candidates"],origin:{lat:number;lng:number}|null,events:PoppyCandidate[]){if(!origin||candidates.length===0)return;const provider=getRoutingProvider();if(!provider)return;const eventById=new Map(events.map(e=>[e.id,e]));const points:{candidate:RecommendationCandidate;lat:number;lng:number}[]=[];for(const c of candidates){const src=eventById.get(c.id);const lat=src?.lat??null;const lng=src?.lng??null;if(lat!=null&&lng!=null)points.push({candidate:c,lat,lng});}if(points.length===0)return;try{const results=await provider.getDriveTimes(origin,points.map(p=>({lat:p.lat,lng:p.lng})));for(let i=0;i<Math.min(results.length,points.length);i++){const minutes=results[i]?.durationMinutes;if(typeof minutes==="number"&&Number.isFinite(minutes))points[i].candidate.driveMinutes=Math.round(minutes);}}catch(err){console.error("[poppy] drive-time enrichment failed",err);}}
